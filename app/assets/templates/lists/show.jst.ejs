<h2><%= list.escape('title') %>: ord-<%= list.get('ord') %></h2>

<ul class="list-group">
  <% list.cards().each(function(card) { %>
    <li class="list-group-item"> <%= card.escape('title') %>: id-<%= card.id %></li>
    <% }) %>
</ul>
