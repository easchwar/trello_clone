<h1>Boards Index</h1>

<ul class="list-group">
  <% boards.each(function(board) { %>
    <li class="list-group-item">
      <a href="#boards/<%= board.id %>"><%= board.escape('title') %></a>
    </li>
  <% }); %>
</ul>
<button class="button-create btn btn-primary">New Board</button>
